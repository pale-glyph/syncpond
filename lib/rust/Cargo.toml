[package]
name = "syncpond-protocol"
version = "0.2.0"
edition = "2021"
description = "Syncpond app protocol types and parsing utilities"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
